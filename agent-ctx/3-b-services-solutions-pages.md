# Task 3-b — Services, Industries & Solutions Pages

Agent: services-solutions-pages (frontend, Dyotanya sketchbook style)
Scope: 6 client page components in `src/components/pages/` — no existing files modified.

## Files created

1. **`services.tsx`** (`#/services`) — PageHero "Eight crafts. One *obsessive* team." (parenthetical "pick one, or let us compose a team") + hero PillButtons (/quote solid, /solutions outline) + animated swirl squiggle. GET `/api/services` → staggered 3-col grid (home.tsx ServiceCard pattern mirrored exactly: CircularImage 110, TrophyBadge on first 3, floating serif index numeral, serif 26px hover-italic title, tagline, coral "Explore →" → `/services/<slug>`; lg:translate-y stagger i%3). LoadingState "Laying out the services…" + EmptyState/failed fallback. "How we work" charcoal band: 4 steps (01 Discover — two weeks of honest questions / 02 Design — architecture & success metrics / 03 Build — weekly demos / 04 Ship & support) with white circle nodes + dusty `Squiggle variant="dash"` (420×150, w-full max-w-[420px] h-auto — aspect-true stretch) running above the grid. Tech-stack `Marquee slow` (Python, PyTorch, dbt, Airflow, Spark, Kubernetes, Terraform, Next.js, Snowflake, Postgres) with sr-only fallback text. Inverted CTA SketchCard "Not sure what you *need*?" → /quote + /contact with coral DoodleStar + dusty underline squiggle.

2. **`service-detail.tsx`** (`#/services/:slug` — all 8 slugs) — `slug = useSegments()[1]`, validated against KNOWN_SLUGS (ai-development, data-analytics, data-science, machine-learning, cloud-devops, web-development, mobile-development, cybersecurity). Pre-hydration (!slug) → LoadingState; invalid slug or fetch 404/fail → DoodleStar + EmptyState "This service page is still being sketched" + PillButton "All services" → /services. Fetches detail + list via Promise.allSettled with alive-flag cleanup; related = list minus current, take 3. Layout: PageHero (`Service — NN` index, TitleWithEm helper italicises last word of title for the roman/italic mix, tagline as parenthetical) → hero row (CircularImage 200 + TrophyBadge + giant ghost order numeral + wiggling DoodleStar, description, "N capabilities · N deliverables" caption, solid "Start a project") → "01 — The work" (longDescription split "\n\n" as 15px/1.75 tracking-tight max-w-2xl paragraphs) → "02 — Capabilities" (2-col SketchCards, Check lucide strokeWidth 1.5 in dusty circle badges) → "03 — Deliverables" (numbered serif 01–NN ledger with border-ink/10 rows) → "Engagement snapshot" charcoal band (3 StatBlocks: Timeline 4–12 wks / Team 3–6 people / Model Fixed or T&M, `[&_span]:text-paper` label fix + dusty wave squiggle) → "04 — Keep exploring" (3 small related cards, CircularImage 80 + hover-italic titles + coral Explore →, skipped when empty) → inverted CTA "Start with {title}" → /quote. Local SketchTitle helper (serif index + clamp h2 + optional parenthetical + animated underline squiggle) per file.

3. **`industries.tsx`** (`#/industries`) — PageHero "Where our data *lives*." (parenthetical "four fields we know deeply…") + animated loop squiggle. GET `/api/industries` → alternating rows (CircularImage 240, md:flex-row-reverse on odd, ghost index numerals, DoodleStar on row 1): serif clamp(28-42px) title, first description paragraph, stats as StatBlock row, first 4 useCases as dusty Tags, coral "Explore {title} →" → `/industries/<slug>`. LoadingState "Unfolding the industry notes…" + EmptyState fallback. Closing tone="paper" CTA card "Don't see your *industry*?" / "(that's fine — data is data)" → /contact.

4. **`industry-detail.tsx`** (`#/industries/:slug` — healthcare, finance, retail, manufacturing) — slug from useSegments; fetches LIST endpoint `/api/industries` only, finds by slug; missing/failed → EmptyState "This field is still being sketched" + "← All industries" pill. CASE_MATCHERS regex map (healthcare→/healthcare/i, finance→/fintech|finance|banking/i, retail→/retail|e-commerce/i, manufacturing→/manufacturing/i) matched loosely against `/api/case-studies` industries, fallback to first case study (verified live data: Retail/Healthcare/Fintech/Manufacturing all present). Layout: PageHero "The *{title}* field guide." → hero split (CircularImage 230 + DoodleStar / stats grid of StatBlocks) → "01 — The landscape" (description paragraphs) → "02 — Use cases" (serif 01–NN numbered ledger, thin connecting border-ink/10 lines) → "03 — Proof" (small case-study SketchCard: CircularImage 96 + TrophyBadge, industry/year Tags, hover-italic title, coral "Read the story →" → /portfolio/<slug>) → inverted CTA "Let's talk about your *{title}* data." → /quote + /services.

5. **`solutions.tsx`** (`#/solutions`) — NO API (per brief): 3 solutions hardcoded with the exact supplied data (data-platform / ai-assistants / analytics-cloud — brand/product split for serif italic mix, taglines, images blog-2/ai-lab/data-viz.png, 6 capabilities each, 3 outcomes each incl. "−40%", "62%", "2.4×" values). PageHero "Solutions, not *science projects*." Alternating large cards (CircularImage 220, ghost numerals, DoodleStar on #2): serif clamp title with em product, tagline, capability Tag pills, outcomes StatBlocks, outline "Explore {product} →" PillButton → `/solutions/<slug>`. "Composable by design" section: 3 SketchCards (Plug into your stack / Start small, scale fast / Own your data — lucide Plug/Sprout/KeyRound strokeWidth 1.5 in dusty circles, 2 sentences each, middle card md:translate-y-6). Inverted CTA "Let's compose your *stack*" → /quote with inline coral link to /services.

6. **`solution-detail.tsx`** (`#/solutions/:slug` — 3 slugs) — hardcoded rich detail (no API): each solution gets 3 substantive paragraphs, the 6 capabilities, 3 outcomes, 5 "What's inside" bullets (title + 1-liner), 3 "Ideal for" criteria, 2 Q&As; plus shared SHIP_STEPS (Week 0 discovery → Week 2 skeleton live → Week 6 v1 shipped → Ongoing iterate). Unknown slug → EmptyState + "← All solutions"; !slug (router pre-hydration) → LoadingState. Layout: PageHero `Solutions — NN` + brand/*product* title + tagline → hero split (CircularImage 220 + ghost numeral + DoodleStar / outcomes StatBlocks) → "01 — The pitch" paragraphs → "02 — Capabilities" 2-col Check cards → "03 — What's inside" numbered serif ledger with sans sub-text → "04 — Ideal for" 3 Check cards → "05 — How it ships" 4 numbered circle steps under dash squiggle → "06 — Fair questions" 2 Q&A SketchCards (serif italic quoted Q, sans A) → inverted CTA "Start with *{product}*" → /quote.

## Style contract compliance
- Every file: `"use client"` + default export, bg-paper via global shell, flex-col root, PageHero top, Section rhythm.
- Serif font-light headings with `<em className="font-normal">` italic mix everywhere; sans tracking-tight text-charcoal body; parentheticals "(like this)"; index markers "01 — Name"; coral only for link/accent text (link-coral); SketchCard/PillButton/Tag/StatBlock from the shared kit; 1.5px-ink CircularImages only (no next/image, no raw <img>); lucide icons strokeWidth 1.5 used sparingly (Check, Plug, Sprout, KeyRound); squiggles/DoodleStars woven subtly (underline on every section title, dash for processes, wave in charcoal bands).
- Robustness: alive-flag effect cleanup, LoadingState on every fetch + router pre-hydration, EmptyState + back-pill on every failure path — pages always render.

## Verification
- `./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0 errors in src/ (only pre-existing errors in unrelated examples/ + skills/ folders).
- Unused-import scan → all imports used (ReactNode/icons flagged were false positives from a naive script; verified by grep).
- Live API smoke tests: GET /api/services, /api/services/ai-development, /api/industries, /api/case-studies all return the exact shapes consumed.
- dev.log: previous "Can't resolve './solution-detail' (and service-detail/industry-detail)" integrator errors are resolved by these files; latest compile "✓ Compiled in 63ms". Registry wiring remains the integrator's job (not touched).
- No dev server / lint / installs run; no existing files modified.

## Integration note for registry owner
Routes to register (filenames match the registry imports already seen in dev.log):
- `/services` → `./services` · `/services/:slug` → `./service-detail`
- `/industries` → `./industries` · `/industries/:slug` → `./industry-detail`
- `/solutions` → `./solutions` · `/solutions/:slug` → `./solution-detail`
(All default exports, no props.)
